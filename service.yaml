apiVersion: v1
kind: Service
metadata:
  name: dollhaven-backend
spec:
  selector:
    app: dollhaven-backend
  ports:
    - protocol: TCP
      port: 8000         # Port to expose to the cluster
      targetPort: 8000   # Port your container is listening on
  type: ClusterIP

